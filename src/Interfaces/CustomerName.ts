//This may seem pedantic, but it ensures that you would have all instances of names comply with honorifics or titles if needed in the future.
export default interface CustomerName {
  name: string;
}
