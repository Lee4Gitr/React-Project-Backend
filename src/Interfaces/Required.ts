// This has shown up in the schema more than once, and looks like an Enum of some type.
// I'm adding this here to show that it could be modeled more thoroughly if necessary.
export default interface Required {
  required: Array<Number>;
}
